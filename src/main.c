#include <raylib/raylib.h>
#include <graph.h>

const int screenWidth = 800;
const int screenHeight = 450;

int main(void) {
    // InitWindow(screenWidth, screenHeight, "ddnn");
    //
    // SetTargetFPS(60);
    //
    // while(!WindowShouldClose()) {
    //     BeginDrawing();
    //         ClearBackground(RAYWHITE);
    //         DrawText("abcdxyz", 190, 200, 20, LIGHTGRAY);
    //     EndDrawing();
    // }
    //
    // CloseWindow();

    return 0;
}
